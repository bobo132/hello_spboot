spring:
  profiles:
    active: dev
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/demo3?autoReconnect=true&useUnicode=true&characterEncoding=utf8
    driver-class-name: com.mysql.jdbc.Driver
server:
  address: 80

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package:
